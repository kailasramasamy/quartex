{
  "name": "quartex",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:web": "turbo dev --filter=@quartex/web",
    "dev:api": "turbo dev --filter=@quartex/api",
    "dev:admin": "turbo dev --filter=@quartex/admin",
    "build": "turbo build",
    "build:web": "turbo build --filter=@quartex/web",
    "build:admin": "turbo build --filter=@quartex/admin",
    "lint": "turbo lint",
    "check-types": "turbo check-types"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "packageManager": "pnpm@10.29.3",
  "pnpm": {
    "onlyBuiltDependencies": ["@parcel/watcher", "esbuild", "argon2"]
  }
}
