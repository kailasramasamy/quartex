{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "cd ../.. && pnpm install --force && pnpm build:web"
  },
  "deploy": {
    "startCommand": "node server.prod.js"
  }
}
